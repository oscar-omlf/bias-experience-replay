defaults:
  - env: frozenlake
  - agents: dqn
  - _self_

seed: 1
device: auto

wandb:
  project: bias-experience-replay
  entity: bias-experience-replay
  mode: online
  group: null
  job_type: train

train:
  total_steps: 200000
  eval_interval_episodes: 10
  checkpoint_interval_episodes: 0
  save_dir: outputs/checkpoints
  