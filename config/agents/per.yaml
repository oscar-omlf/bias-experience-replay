algo: dqn

# Replay / sampling
replay:
  type: per
  capacity: 50000 # 200000
  batch_size: 128 # 128
  eps: 1.0e-6
  alpha: 0.6
  beta: 0.4
  beta_anneal_steps: 500000 # 35000

  sa_mitigation:
    enabled: false
    method: none   # avg | sample | model | none
    include_self: true
    min_group: 1
    max_group: 8 # 64
    update_all_siblings: true

# Exploration
epsilon:
  start: 1.0
  end: 0.05
  decay_steps: 400000 # 130000

# Optimization
optimizer:
  lr: 3.0e-4 # 3.0e-4
  weight_decay: 0.0

# DQN / DDQN specifics
gamma: 0.99  #  1.0  # 0.99
handle_time_limit_as_terminal: true
train_freq: 1              # gradient step each env step
learning_starts: 5000 # 10000
gradient_steps: 1
max_grad_norm: 10.0

target_update:
  interval: 1000 # 1
  tau: 1.0 # 0.01

# Network
model:
  type: mlp
  activation: relu
  hidden_sizes: [64, 64]  # [128, 128]
  conv_channels: [16, 32]
  fc_hidden: 128

# Logging and eVal
log_interval_steps: 2000
eval_episodes: 50
